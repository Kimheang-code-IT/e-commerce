# Docker Compose DNS resolves `backend` to all scaled replicas (round-robin / least_conn).
upstream ecom_backend {
    least_conn;
    server backend:8000 max_fails=3 fail_timeout=30s;
    keepalive 16;
}
