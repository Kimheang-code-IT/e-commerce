# Local secrets (templates live in ../.env.example at repo root)
.env
.env.local
.env.*.local

# Runtime
__pycache__/
*.py[cod]
.pytest_cache/
exports/
uploads/
*.db
*.db.bak-*

# Google / Telegram secrets — copy JSON here on each server (gitignored)
credentials/*
!credentials/.gitkeep
!credentials/README.md
